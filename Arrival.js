class Arrival {
    constructor(lineId, arrivalTime, stationName, naptanId, destination) {
                this.lineId = lineId,
                this.arrivalTime = arrivalTime,
                this.stationName = stationName,
                this.naptanId = naptanId,
                this.destination = destination
    }
}

module.exports = {Arrival}